from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project is all about using the generative adversarial networks architecture with the VAE neural network to generate new 3D data samples using TensorFlow',
    author='bahri khalid',
    license='MIT',
)
